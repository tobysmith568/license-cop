{
  "name": "license-cop-e2e",
  "private": true,
  "scripts": {
    "typecheck": "tsc -b tsconfig.json",
    "lint": "eslint . --max-warnings 0",
    "e2e": "bun test --isolate --timeout=60000 --coverage --coverage-reporter=lcov --coverage-dir=coverage src"
  }
}
