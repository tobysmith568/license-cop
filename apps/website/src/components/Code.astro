---
import { Code as AstroCode } from "astro/components";
import type { Lang } from "shiki";

interface Props {
  code: string;
  lang?: Lang;
}

const { code, lang } = Astro.props;
---

<AstroCode lang={lang} code={code} theme="light-plus" />
