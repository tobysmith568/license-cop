name: Setup
description: Check out the repo, install Node and bun, and install dependencies

inputs:
  ref:
    description: The git ref to check out
    required: false
    default: ""
  node-version:
    description: The Node.js version to install
    required: false
    default: "24"
  registry-url:
    description: The npm registry URL to configure Node for publishing against
    required: false
    default: ""

runs:
  using: composite
  steps:
    - name: Checkout
      uses: actions/checkout@v7
      with:
        ref: ${{ inputs.ref }}

    - name: Install Node
      uses: actions/setup-node@v5
      with:
        node-version: ${{ inputs.node-version }}
        registry-url: ${{ inputs.registry-url }}

    - name: Install bun
      uses: oven-sh/setup-bun@v2
      with:
        bun-version: 1.4.0

    - name: Cache bun install cache
      uses: actions/cache@v4
      with:
        path: ~/.bun/install/cache
        key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
        restore-keys: |
          ${{ runner.os }}-bun-

    - name: Install dependencies
      run: bun install --frozen-lockfile
      shell: bash
